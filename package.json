{
  "name": "classical",
  "version": "1.0.0",
  "description": "A TypeScript object-oriented base class library in the spirit of C#",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && npm run join",
    "join": "node join-types.js",
    "test:node": "cd environments/node && npm run test:env",
    "test:react": "npm run build && cd environments/react && npm install && npm test",
    "test:react-native": "npm run build && cd environments/react-native && npm install && npm test",
    "test": "npm run test:node && npm run test:react && npm run test:react-native"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DNA-Platform/classical"
  },
  "author": "Doug",
  "license": "DNA Platform License",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "typescript": "^4.0.0"
  }
}
